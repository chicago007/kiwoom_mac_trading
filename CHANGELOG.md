# 변경 기록

지금은 **0.22**입니다. 다음은 **0.23**입니다.

## 0.22 — 2026-09-20

종합2 잔고 탭을 되돌리고, 관심종목과 보유를 섞어 보이지 않게 했습니다.

- 종합2 왼쪽: 관심종목 | 잔고 탭. 관심종목 옆에 키움 그룹 이름(예: 보유주식)을 붙이지 않음. 그룹이 둘 이상이면 선택만
- 종합·종합2 주문창: 매도이고 그 종목 잔고가 있으면 수량 옆에 `잔고 N주`. 누르면 수량 채움

## 0.21 — 2026-09-20

종합 주문창을 낮춰 차트를 키우고, 티커 입력과 종합2 왼쪽을 다듬었습니다.

- 주문창: 티커 박스 아래 `구분 [] 수량 []` / `유형 [] 가격 []`. 제목은 칸 옆. 가로는 이전과 같음
- 티커는 직접 입력 가능. 검색·최근 목록은 칸을 클릭하거나 커서가 있을 때만. 잔고·관심에서 고르면 티커만 바뀜
- 종합2 왼쪽은 관심종목만. 잔고(보유) 탭 없음

## 0.2 — 2026-09-19

표시 이름과 면책을 맞추고, 참고 화면처럼 차트가 큰 **종합2**를 추가했습니다. 버전 번호는 0.11에서 0.2로 올립니다.

- 이름: Mac용 키움 REST API 기반 미국주식 수동매매 웹앱. 키움 공식 HTS/WTS/MTS가 아니며 제휴·협력 없음
- 개발자 본인 계좌의 개인 사용. 제3자 계좌 운용·투자자문·투자일임·주문 대행이 아님
- 종합2(`/desk2`): 관심·잔고 왼쪽, 큰 차트, 호가·당일체결, 오른쪽 매수/매도 주문. 색은 종합과 같음
- 종합2 관심·잔고는 티커·현재가·등락가격·등락비율만. 숫자는 오른쪽 맞춤, 가로 스크롤 없이 맞춤

## 0.11 — 2026-09-19

종합 화면을 실제 매매에 맞게 다듬었습니다. 관심종목·기간 거래내역·장 구분·주문 실수 방지를 넣고, 상단 지수는 ETF가 아니라 거래소 지수·선물을 씁니다.

- 종합 상단에 다우·S&P 500·나스닥 종합·미국채 10년·WTI(NYMEX)와 키움 적용환율·시장환율
- 관심종목 등락을 `$`와 `%` 한 줄로 표시. 종목·현재가·등락·거래량 정렬
- 키움 HTS 관심그룹 불러오기/앱 저장. 공식 REST에 쓰기 API 없음
- 티커 자동완성(`usa10098`/`usa10099`), 최근 종목, 거래소 자동
- 뉴욕 기준 프리마켓·정규장·애프터·휴장 표시
- 주문가능수량(`ust31490`)·예수금 대비 경고. 킬스위치면 주문 버튼 차단
- 주문내역 / 당일 체결 / 거래내역 탭. 기간 거래내역은 `ust21100`(HTS와 동일 필드), CSV 저장
- `./dev.sh`로 API와 화면을 한 터미널에서 실행. 안 쓸 때는 Ctrl+C

## 0.1 — 2026-09-19

Mac용 키움 REST API 기반 미국주식 수동매매 웹앱 초기 공개. 맥북·맥에서도 간단한 수동 매매가 가능하다는 점을 알리고, HTS/공식 WTS보다 호가가 늦을 수 있다는 경고를 넣었습니다. `kiwoom_autotrading` 0.12를 준용하고 **자동매매 전략은 제외**했습니다. 0.1은 미국주식 우선이며, 국내주식은 같은 방식으로 확장할 수 있습니다.

- 종합(`/trade`)에 호가·주문·잔고·주문내역·체결·차트
- 현재가 `usa20100`, 10호가 REST `usa20101` + 실시간 `FT`
- 일봉 `usa06012` / 분봉 `usa06011`
- 예수금·잔고·당일 주문/체결. 손익은 키움 손익금액(수수료 반영)
- `KIWOOM_ORDERS_ENABLED=true`이면 확인 후 `ust20000` 계열 주문. 기본은 목업
- 킬스위치, 주문 2단계 확인
- UI 3010 / API 8010 (기존 자동매매 프로젝트와 포트 분리)
- 전략 화면·조건 스케줄러·드라이런 전략 토글 없음
- README에 맥 필수 프로그램(Python 3.13, Node 20, Homebrew)과 설치·실행 순서
- 대시보드 제거. 첫 화면을 종합으로 두고 호가·주문·잔고가 한 화면에 보이도록 레이아웃 수정
- README에 실주문·매매 본인 책임, 최소 수량 실주문 테스트 후 가동 권고
