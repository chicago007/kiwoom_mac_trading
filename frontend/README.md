# 키움증권 맥북용 매매시스템 UI

버전 0.1 Next.js 화면입니다. 브라우저는 키움에 직접 호출하지 않고 `/api/*`를 FastAPI(`127.0.0.1:8010`)로 넘깁니다.

```bash
cd frontend
npm install
npm run dev
```

브라우저에서 http://127.0.0.1:3010 을 엽니다. 종합 화면은 `/trade`입니다.
