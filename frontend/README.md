# Mac용 키움 REST API 기반 미국주식 수동매매 UI

버전 0.11 Next.js 화면입니다. 브라우저는 키움에 직접 호출하지 않고 `/api/*`를 FastAPI(`127.0.0.1:8010`)로 넘깁니다.

```bash
cd frontend
npm install
npm run dev
```

브라우저에서 http://127.0.0.1:3010 을 엽니다. 첫 화면이 종합입니다.
